// Create an array of numbers
var numbers = [1,2,3];

// Create an array to hold doubled numbers
// Double numbers
var doubled = numbers.map((number) => {
    return number * 2;
});

console.log(doubled);